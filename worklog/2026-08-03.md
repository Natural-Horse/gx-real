# 2026-08-03 工作日志

## 工作记录

- 诊断 robodog 开机自动起立：SSH 已连接（unitree@192.168.31.254），检查 systemd/rc.local/cron/autostart，未发现开机自动启动 gx-real 节点或站立的服务。最近两次运行日志 `logs/19700117_051508`、`19700117_051529` 显示节点启动后处于 Passive 阻尼，第一条 `/wirelesscontroller` 消息（05:15:42.992，摇杆全零）即被 `joy_stick_cb` 判定 R1 按下，随后 4 ms 内执行 `start()` internal FixStand 起立序列。根因指向手柄开机/连接首包携带 R1 位，配合 `button_pressed_once` 把首条消息当作新按下触发。待用户确认后在真机执行 `ros2 topic echo /wirelesscontroller --once` 复核 keys 实际值。
- 完成远程 StarVLA 真机链路：回环 WebSocket 协议客户端、工作站双跳 SSH 隧道、离线/在线 shadow、在线 ROS runner、NAV waypoint 速度适配、WBC `external_vla` 速度源、X5 外部 base-frame TCP 源、双层 workspace/step gate、底盘/机械臂 watchdog 和 JSONL 日志。
- 增加三个固定启动入口；live 模式默认关闭，必须显式双重确认。远程客户端不打开 CAN，高层权重不覆盖 Go2 底层 ONNX。
- 重写 `docs/实机测试指南.md`，保留必要硬件检查、完整启动/停止顺序和九级放行流程。
- robodog 不能访问 GitHub 的根因不是 DNS：缺少默认路由且系统时间为 1970；换 DNS 不能单独解决。
- 静态回归：VLA/控制相关全套 `89 passed, 1 skipped`；全仓另有 28 项高度扫描测试因缺失 `policies/height_scan_contract.yaml` 无法运行，与本次改动无关。
- 实机部署链路只读审计（远程 zju-server + robodog）：
  - zju-server A40 3/4、RTX 3090 5/6 空闲；远端 `robodog` 分支含 `go2_remote` 与 `start_go2_vla_server_tmux.sh`；`go2_n200_nav_action_a40x2_b1_0802/final_model` 的 config.yaml 为 `action_dim=10, state_dim=10, action_horizon=4, flow_matching`。
  - 缺口 1：该 run 目录缺少 `dataset_statistics.json`，`read_mode_config` 断言缺失会导致 `from_pretrained` 启动失败，需补文件或放宽加载。
  - 缺口 2：robodog 上 gx-real 停留在 `83d53a2`，缺少 `6f4b3b6` 的 VLA 客户端、`vla_remote_protocol`、`vla_safety_adapters` 与 WBC `external_vla` 支持，需 rsync/bundle 同步。
  - 缺口 3：robodog SSH 配置禁止 pubkey（`PreferredAuthentications password`），隧道脚本 `ssh -fNT -R robodog` 会卡密码；需改 pubkey 或 sshpass。robodog 无默认路由但反向隧道由工作站发起，无需机器狗出网。
  - 缺口 4：robodog 无 `/dev/video*`（相机未枚举）、`websockets` 未确认安装；`base_velocity_body` 客户端硬编码零、机械臂状态用 target 代理，与训练 state 有偏差。
  - 量纲结论：go2 flow-matching 路径不归一化，`predict_typed_action` 不做反归一化，输出即物理量（m/rad + gripper 0..1），与 gx-real 适配器假设一致。
- 完善实机部署链路：robodog SSH 已配置本机 `id_ed25519` 公钥免密（密码 123）；将世界系锚定 `WaypointAdapter` 移植到 `real-wbc/modules/waypoint_adapter.py`（自包含、无 starVLA 依赖）并接入 `vla_real_client.py`（`--nav-anchor-mode body|odom`，odom 模式需要 `nav_msgs/Odometry`），新增单测 `tests/test_waypoint_adapter.py`（4 passed）。
- 将 83d53a2..b90f857 的 27 个变更文件 rsync 到 robodog（VLA 客户端、safety adapters、waypoint adapter、隧道脚本、启动脚本、worklog）；robodog 上关键文件已核验。提交 `b90f857` 已推送 `Natural-Horse/gx-real` fork。

## 全日总结

- 真机远程评测代码路径已补齐并通过静态测试；真实相机、Go2 和 X5 联合动态测试仍须按指南逐级现场验收。
- robodog 自动起立诊断为手柄首条消息携带 R1 触发 internal FixStand；待确认手柄 keys 实际值并决定是否增加首包/去抖防护。
- 实机部署审计完成：服务端可启动但缺 `dataset_statistics.json`；robodog 缺 VLA 客户端代码、pubkey 隧道认证与相机/依赖；NAV 速度适配与 X5 IK 控制路径均已就位，待现场 6--9 级验收。
- robodog 公钥免密已配好；world-frame waypoint adapter 已入 gx-real 并接线；gx-real 最新代码已同步 robodog。下一步：真机相机/依赖确认与分级放行 6--9 现场验收。
