# 2026-08-06 工作日志

## 工作记录

- 部署链路真机闭环验证：隧道（zju-server + robodog 两端）正常；
  robodog 上 `publish_real_cameras.py`（front=/dev/video4, wrist=/dev/video10）
  发布 JPEG topic，`run_vla_real_interactive.py`（shadow）经隧道连 r8 推理服务，
  真实相机图像连续产出 route/subtask/waypoint 决策（~1.1-2.0s/次），无移动。
- 测试后清理 robodog 的 vla_cams / vla_client 会话。
- 排查"机械臂位姿脚本移动后无变化"：`inspect_arm_tcp_pose.py` 是离线 FK/IK
  工具（argparse 明确 Does not use ROS or hardware），不连实机；现场 `/arm/state`
  无数据原因是相机/臂线未插好，`calibrate.py X5 can0` 复测正常。
- 阅读 `docs/完整指南.md`（legacy spacemouse 完整手册，含 MCF 释放、CAN 配置、
  calibrate 自检、最小上机清单），重写 `docs/实机测试指南.md` 为 VLA 完整流程：
  开机自检 -> 启动准备（prepare_real_run/MCF/calibrate/相机）-> 服务器与隧道 ->
  robodog 四节点启动 -> shadow 拿推理结果 -> live 执行 -> 分级放行/停止/故障/日志。
- 夹爪标定修复：SDK X5 默认 open_readout=5.03 与旧常量 -5.07839 均与实机不符；
  用 SDK `calibrate_gripper` 实测 fully-open readout=**5.06123**，更新
  `X5_GRIPPER_OPEN_READOUT`（闭合零位已写入电机，解决"只能开 5cm/读数偏移 3.8cm"）。
- 夹爪标定最终方案：确认本机夹爪闭合零位**掉电/SDK 重启后丢失**（静止位
  readout≈-1.265，按正确 open_readout=5.13142 换算得 -0.022m，SDK 启动检查
  直接 Emergency）。新增 `scripts/calibrate_gripper.sh`：临时 open_readout=-3.0
  绕过启动检查 -> calibrate_gripper（闭合按回车 -> 全开按回车）-> 保持通电立即
  启动臂节点；已同步 robodog，`bash -n` 通过。`完整指南.md` 8.3 节补充上电标定
  流程与 readout 常量注释说明零位易失。
- 夹爪标定改为软件零位偏移（用户建议：记住标定值、启动时赋值，不再每次上电
  交互标定）：SDK fork 增加 `RobotConfig.gripper_zero_offset` 字段（默认 0.0），
  读路径 pos=(angle-zero_offset)/open_readout*width、命令路径
  motor_target=zero_offset+pos/width*open_readout，pybind/pyi 同步暴露。常量收进
  新模块 `real-wbc/modules/arx5_gripper_calib.py`（实测同会话换算：
  raw_open=-1.2696、raw_closed=-6.348、span=5.0784），spacemouse_arm_node 与
  wbc_node_leg12_arm_passthrough 启动时统一写入。已在 robodog 重编
  arx5_interface.so（Jetson 编译 ~30s，strip 后安装，旧 .so 备份为
  /home/unitree/arx5_interface.so.bak_20260806），无硬件验证字段读写通过；
  `/arm/state` 闭合应≈0、全开应≈0.088，等待实机复测。
- 实机闭环测试准备：`run_vla_real_interactive.py` 增加动作确认门控——首次推理、
  route 与上一次不同、上一动作被拒绝三种情况在 client 终端阻塞，打印下一个
  预测动作（nav waypoint / grasp-place TCP 目标）并询问 0=否 1=是，期间不发
  新推理；同 route 连续执行不再重复询问。同时修复两个安全缺口：live 输出必须
  `real.enable_live_output=true` + `confirm_live_output=I_UNDERSTAND_LIVE_OUTPUT`
  双确认（此前只在文档、代码未强制）；shadow 模式下 NAV 也不再发布
  `/vla/base_cmd`（此前 shadow 也会发速度、WBC 一开狗就会动）。确认逻辑
  5 项行为测试通过；`real_go2_x5.yaml` 与实机测试指南同步更新。
- 统一配置文件与启动器改造：`configs/vla_eval/real_go2_x5.yaml` 按模块组织
  （server/task/camera/leg/arm/client/interactive），新增
  `interactive.confirm_nav` / `interactive.confirm_arm` 开关（默认 true，置 false
  关闭对应动作的阻塞确认，机械臂仍受安全门控）；新增
  `scripts/vla_config_args.py` 按模块生成节点启动参数，`run_vla_real_quad.sh` 与
  `run_vla_real_all.sh` 均从统一 yaml 读取并支持 `--override` 透传给 client；
  client 读取 `client` 模块（兼容旧 `real` 键）。`docs/实机测试指南.md` 按最新
  流程整体重写：完整启动顺序（趴卧->prepare_real_run->验 X5->隧道->起四节点->
  R1 起身->L2 policy->client 逐条确认）、交互确认规则、安全边界与可靠性说明、
  L1 两次急停/硬件急停/Ctrl+C 行为、停止顺序与分级放行。
- 新增 `scripts/run_vla_real_quad.sh`：单 tmux 会话 2x2 四宫格同时跑
  cams/leg/arm/client；新增 `scripts/view_cameras_rviz.sh` + `configs/rviz/cameras.rviz`：
  一键起相机+解压节点并打开 rviz2 显示两路图像；新增 `scripts/decompress_cameras.py`
  （CompressedImage -> Image，rviz2 需要）。
- `docs/实机测试指南.md` 补入四宫格启动、rviz 一键查看、夹爪标定说明并同步 robodog。

## 全日总结

- 相机->topic->client->隧道->server 全链路通畅；实机测试指南已按 VLA 完整流程重写。
- 夹爪标定修正（5.06123）、四宫格启动与 rviz 相机查看器已交付并同步 robodog。
- 夹爪零位易失结论确认：已用 SDK gripper_zero_offset 软件偏移解决，日常启动
  不再需要交互式标定，换臂时才用 calibrate_gripper.sh 重测。
- client 交互确认门控与 live 双确认落地，shadow 不再发布任何动作；待实机闭环。
- 参数全部收敛到单 yaml（模块化），新增机械臂确认开关，实机测试指南已重写。
