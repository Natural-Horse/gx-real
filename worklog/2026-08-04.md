# 2026-08-04 工作日志

## 工作记录

- 实现实机 VLA 交互评测 client（yaml 驱动）：
  - 新增 `configs/vla_eval/real_go2_x5.yaml`：server endpoint、instruction、ROS topic、nav_anchor_mode、tolerance、arm 人工门控提示等全部可调。
  - 新增 `scripts/run_vla_real_interactive.py`：NAV 只执行第一个 waypoint（odom 锚定走到位或 body 比例速度），GRASP/PLACE 终端阻塞等待数字输入（1 执行 / 0 跳过），期间不发起新推理，实时打印并写 JSONL；不启动场景、不重置位置。
  - 提交 `a9842e8` 已推送 `Natural-Horse/gx-real`（origin mtr_dev）。

## 全日总结

- 实机交互 VLA client 已实现并编译/静态测试通过；NAV 单 waypoint + 人工门控逻辑与仿真一致。
- 待现场：相机 topic、websockets 依赖、odom 源确认；真机分级放行 6--9 验收。
